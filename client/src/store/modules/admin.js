const state = {
  allNotes: [],
};

const getters = {
  getAllNotes: (state) => state.allNotes,
};

const actions = {};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations,
};
